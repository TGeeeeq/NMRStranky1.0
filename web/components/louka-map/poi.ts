import type { XY } from "./map-data";

/** Zajímavá místa na mapě — přidání místa = jeden řádek tady.
 *  Souřadnice jsou v souřadnicích mapy (viewBox 0–1000 × 0–výška);
 *  orientačně: [0,0] je severozápadní roh výřezu. */
export const pois: { name: string; at: XY }[] = [
  // { name: "Ohniště", at: [420, 610] },
];
